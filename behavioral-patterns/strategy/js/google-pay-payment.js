/**
 * Processes a payment via Google Pay.
 *
 * @param {number} amount - The amount to be paid.
 */
function googlePayPayment(amount) {
  console.log(`Processing Google Pay payment of $${amount}`);
};
