/**
 * Initiates the delivery process for the ordered dish.
 * @param {string} dish - The name of the dish to deliver.
 */
export function deliverOrder(dish) {
  console.log(`Delivering ${dish} to your address...`);
}
